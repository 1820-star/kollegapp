{
  "name": "kollegapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler deploy",
    "deploy:dry": "npx wrangler deploy --dry-run"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.0.0"
  }
}
